# 📊 Система успеваемости (Docker + PostgreSQL)

Проект переведён на промышленный стек: **Docker**, **PostgreSQL**, **Gunicorn**.

## 🔧 Требования

- Docker 20.10+
- Docker Compose v2.10+

## 🚀 Запуск (разработка)

1. Скопируйте `.env.example` → `.env` и заполните:
   ```bash
   cp .env.example .env